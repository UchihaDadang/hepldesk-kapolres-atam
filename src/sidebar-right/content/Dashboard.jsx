import ModalHeader, { HeaderDashboard } from "../../modal/ModalHeader";

export default function Dashboard() {
    return (
        <div className="dahsboard-page">
            <ModalHeader>
                <HeaderDashboard/>
            </ModalHeader>
        </div>
    )
}